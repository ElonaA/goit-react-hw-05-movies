import Navigation from '../../Navigation/Navigation';
import { Inner } from './Header.styled';

const Header = () => (
  <header>
    <Inner>
      <Navigation />
    </Inner>
  </header>
);

export default Header;
